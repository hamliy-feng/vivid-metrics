<!-- codex-memory:template=workflow-rules:v1 -->

# 工作流规则

- `current.md` 只保留当前有效信息，必须覆盖更新
- `archive/` 只保留历史过程，不承担当前交接职责
- 大型任务使用 `tasks/active/<task>/`
- 任务完成后转入 `tasks/archive/`
- 当聊天变长、任务变复杂、准备切换阶段、或准备结束当前线程时，应先更新 `current.md`
